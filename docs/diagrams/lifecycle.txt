                          ╔══════════════════════════════════════╗
                          ║   LIFECYCLE / slash command flow    ║
                          ╚══════════════════════════════════════╝

  USER (in Discord)        HERMES GATEWAY                BRIDGE
  ────────────────         ───────────────                ──────
       │
       │  /voice-vapi
       ├────────────────────►│
       │                     │  register_tool lookup
       │                     │  voice_vapi()
       │                     │
       │                     │  force-disconnect any
       │                     │  existing vc in guild
       │                     │  (Gemini↔Vapi safety)
       │                     │
       │                     │  importlib.load(bridge.py)
       │                     │
       │                     │  create_task(run_sidecar)
       │                     ├─────────────────────►│
       │                     │                       │
       │                     │                       │  channel.connect()
       │                     │                       │  (~27s due to CDN
       │                     │                       │   handshake quirk)
       │                     │                       │
       │                     │                       │  open Vapi WSS
       │                     │                       │  send `start` event
       │                     │                       │
       │                     │                       │  start VoiceListener
       │                     │                       │  start LiveAudioSource
       │                     │                       │  start sidecar HTTP
       │                     │                       │
       │                     │                       │  resolve ready_future
       │                     │  ◄─────────────────────┤
       │                     │  { ok: True, vc: ... }│
       │                     │
       │  "Voice bridge      │
       │   started in        │
       │   #channel-name"    │
       │  ◄───────────────────┤
       │                     │
       │  (conversation)     │
       │  ◄──────────────────►│  ◄──────────────────►│  ◄──► Vapi
       │                     │                       │
       │  /voice-vapi-leave  │
       ├────────────────────►│
       │                     │  bridge task cancel
       │                     ├─────────────────────►│
       │                     │                       │  send `stop` event
       │                     │                       │  close WSS
       │                     │                       │  vc.disconnect()
       │                     │                       │  HTTP server shutdown
       │                     │                       │
       │                     │  pop _active_bridges  │
       │                     │  ◄─────────────────────┤
       │  "Bridge stopped"   │
       │  ◄───────────────────┤


  IDEMPOTENT RE-ENTRY:

    /voice-vapi  ──►  same channel?    ──►  "success: ready"   (no-op)
                   └─ different chan?  ──►  vc.move_to()       (no disconnect)
                   └─ stale entry?     ──►  cancel + start fresh


  AUTOSTART (on gateway boot):

    voice-vapi-autostart.json exists?
        │ yes
        ▼
    _schedule_autostart_thread()
        │
        ▼
    180s deadline loop:
        - find adapter
        - read params
        - call voice_vapi() on gateway loop
        │
        ▼
    success → delete autostart file
