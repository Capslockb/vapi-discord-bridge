                       ╔════════════════════════════════════════════╗
                       ║        vapi-discord-bridge                ║
                       ║            Dataflow overview              ║
                       ╚════════════════════════════════════════════╝

  ┌──────────────────┐         ┌──────────────────────┐
  │   USER SPEAKS    │         │   VAPI REPLIES        │
  │   (microphone)   │         │   (TTS audio)         │
  └────────┬─────────┘         └──────────┬───────────┘
           │                              ▲
           │ Opus (48 kHz stereo)         │ PCM (24 kHz mono)
           ▼                              │
  ┌──────────────────┐         ┌──────────┴───────────┐
  │  Discord Voice   │  WSS    │    VapiClient        │
  │  (UDP + NaCl)    │◄───────►│  (asyncio task)      │
  └──────────────────┘         └──────────┬───────────┘
                                          │ tool calls
                                          ▼
                               ┌──────────────────────┐
                               │  Function dispatcher  │
                               │  (in-process)         │
                               └──────────────────────┘
                                          │
                                          ▼
                               ┌──────────────────────┐
                               │  ~/.hermes/voice-     │
                               │  vapi-notes/*.jsonl   │
                               │  (transcript)         │
                               └──────────────────────┘


  HOT PATH (audio):       VoiceListener ─► Vapi WSS ─► LiveAudioSource
  COLD PATH (control):    HTTP /health, /say, /leave on 127.0.0.1:18944
  OBSERVABILITY:          bridge.py → BRIDGE.health() → JSON dict
